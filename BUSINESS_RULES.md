# AStock-AI-Workbench V1.0 业务规则

## 1. 适用范围与共同约定

本规则适用于用户手工维护的 A 股普通股票和 A 股 ETF 持仓与交易流水。两者使用统一证券模型，并以 `instrument_type` 区分品种与适用交易规则。应用是本地投研和记账工具，不接入券商交易权限，也不发送交易指令。

- 交易日、交易时段与证券市场使用 `Asia/Shanghai`；展示时间应附带时区语义。
- 金额、价格和数量以精确 decimal/定点值核算，不使用二进制浮点数作为权威计算结果。
- 所有外部行情、资讯和事件均须保留来源、数据时间与抓取时间；无法确认时显示“暂无数据”或“未确认”。
- V1.0 支持 A 股普通股票和 A 股 ETF；暂不支持港股、美股、债券、期货、期权、融资融券、可转债及税务申报。
- V1.0 可展示分红、送转、除权除息公告，但不自动调整持仓数量、成本或盈亏；公司行动自动账务调整延后至 V1.1。

## 2. 持仓成本规则：移动平均成本法

V1.0 采用**移动平均成本法**，成本按“账户 + 证券”独立计算。数据库必须保留不可替代的完整交易流水；当前持仓数量、平均成本和盈亏均是可重算的派生结果。

### 2.1 定义

- `Q`：交易前持仓数量。
- `C`：交易前平均持仓成本（每股）。
- `B`：本次买入数量。
- `P_buy`：本次买入成交单价。
- `F_buy`：本次买入费用总额，即佣金、过户费、其他费用之和。
- `S`：本次卖出数量。
- `P_sell`：本次卖出成交单价。
- `F_sell`：本次卖出费用总额，即佣金、印花税、过户费、其他费用之和。

所有费用均以独立字段记录并以实际流水为准；未知费用不得默认为零而不作标记。界面应显示“费用未确认”并限制相应收益结果的可信度说明。佣金须支持最低佣金规则：若用户启用了该规则，买入或卖出佣金为“按佣金费率计算值”和“最低佣金”中的较高者；实际发生佣金可由用户覆盖，但覆盖原因应保留在流水审计信息中。

### 2.2 买入与加仓

首次买入后的持仓总成本为：

```text
持仓总成本 = B × P_buy + F_buy
平均成本 C_new = 持仓总成本 ÷ B
```

已有持仓时，加仓后的移动平均成本为：

```text
C_new = (Q × C + B × P_buy + F_buy) ÷ (Q + B)
Q_new = Q + B
```

**买入端手续费计入持仓成本。** 平均成本按展示精度四舍五入仅用于展示；内部应保存足够精度以保证后续卖出与清仓可复算。

### 2.3 卖出

卖出不会改变剩余股份的平均成本：

```text
卖出对应成本 = S × C
Q_new = Q - S
C_new = C（仅在 Q_new > 0 时）
```

卖出端费用不计入剩余持仓成本，而在已实现盈亏中扣除。卖出前必须通过可卖数量和 T+1 校验。

### 2.4 清仓

当 `Q_new = 0` 时：

- 以该次卖出计算最终已实现盈亏；
- 当前持仓数量归零，平均成本和未实现盈亏设为“无持仓/不适用”，不得显示为可继续使用的成本价；
- 保留证券、交易流水与历史盈亏记录，以支持审计、查询和未来重新建仓；
- 后续再次买入视为新的持仓周期，按首次买入规则重新计算平均成本。

### 2.5 期初持仓与历史导入

- V1.0 支持期初持仓录入。期初记录必须包含账户、证券、基准日期、持仓数量、可卖数量和平均成本；可选记录现金余额及费用确认状态。期初持仓作为可审计的期初依据，不能被普通持仓编辑无痕覆盖。
- 期初持仓的可卖数量不得超过持仓数量；未提供取得日期时，以用户确认的基准日前已取得处理，且应记录“期初录入”来源。
- V1.0 支持历史交易流水导入。导入前须执行字段、日期、证券、数量、价格和费用校验；通过后逐笔保留原始导入标识、导入时间、来源文件引用/哈希及校验结果。
- 导入的买卖记录与手工录入的买卖记录使用同一移动平均成本法重算，不得以导入汇总值覆盖完整流水。

## 3. 盈亏规则

“当前价格”必须是有效行情价格；行情无效、过期或未确认时，与其相关的市值、今日盈亏和浮动盈亏显示“暂无数据/未确认”，而非以零替代。

| 指标 | V1.0 公式与口径 |
| --- | --- |
| 今日盈亏 | `数量 ×（当前价格 - 上一个有效收盘价）`。数量为当前持仓数量；上一个有效收盘价应来自同一证券、同一市场的可追溯行情。 |
| 浮动盈亏（未实现盈亏） | `数量 ×（当前价格 - 持仓成本）`。其中持仓成本为移动平均成本，已含历史买入端费用。 |
| 已实现盈亏 | `卖出金额 - 对应成本 - 交易费用`，即 `S × P_sell - S × C - F_sell`。 |
| 总收益 | `已实现盈亏 + 未实现盈亏`。缺少有效当前价时，总收益为未确认，不以已实现盈亏单独冒充总收益。 |

说明：买入端费用已进入移动平均成本，不能在卖出/总收益中再次扣减。V1.0 的“今日盈亏”是价格变动口径，不含当日交易费用；若未来增加“今日净盈亏”指标，必须另行定义并标注口径。

## 4. A 股交易规则

### 4.1 T+1 与可卖数量

- A 股普通股票按 T+1 规则处理：**当日买入不可当日卖出**。
- 可卖数量为截至该交易日开盘前（或交易发生前按交易日规则）已买入、尚未卖出且取得日早于当日的数量之和。
- 卖出数量必须大于零、为有效交易单位且不超过可卖数量；不满足时拒绝写入交易流水，并说明原因。
- A 股 ETF 使用同一证券与交易流水模型，但卖出可用规则必须按该 ETF 的交易所/基金官方规则确认后写入证券规则。不得把普通股票的 T+1 规则盲目套用于所有 ETF；规则未确认的 ETF 可展示行情和持仓，但不得启用卖出流水，界面显示“未确认”。

### 4.2 资金可用规则

- 买入记录须校验账户的“可用于买入现金”足够覆盖 `B × P_buy + F_buy`。
- 卖出成交所得在本地账本中**当日可用于再次买入**；`可用于买入现金`随卖出净额增加。
- 卖出所得的“可取现金”按下一个交易日结算可用处理（T+1）；V1.0 不连接券商，因此此字段仅为记账提示，不能表示真实券商余额。
- 现金分为可用于买入、可取与待结算三个口径时，界面须明确标注；若用户未维护足够信息，显示“未确认”。

### 4.3 交易时间与节假日

- 正常交易日的集合竞价为 09:15–09:25，连续竞价为 09:30–11:30、13:00–15:00（北京时间）。
- 交易时间校验以交易所发布的交易日历与公告为准；交易日历缓存于本地 SQLite，应用启动时检查并更新。临时停市、半日市或证券停复牌应覆盖默认时段。
- 周末、法定节假日和交易所公布的非交易日不得新增“当日成交”流水；历史修正需采用明确的人工补录流程并保留审计说明。
- 交易日历或临时安排不可用时，优先使用有版本和更新时间的本地缓存；缓存不存在或不能确认时，新交易流水日期状态为“未确认”，不得自动假定为有效交易日。

## 5. 行情规则

### 5.1 行情状态

每条股票、指数或 ETF 行情必须显示状态、数据时间（`market_timestamp`）、抓取时间（`fetched_at`）和数据源（`source`）。状态定义如下：

| 状态 | 条件 | 展示与计算规则 |
| --- | --- | --- |
| 实时 | 仅限合法授权的实时数据源明确标识为实时，且在连续竞价/集合竞价期间，`系统时间 - 数据时间 ≤ 1 分钟` | 可作为当前价格用于估值；仍显示来源与时间。 |
| 延迟 | 公开行情或数据源标为延迟，或交易时段内数据时间落后 1–5 分钟 | 可展示，但须标注延迟；界面禁止将延迟行情称为实时。是否计入估值由用户可见的估值口径决定。 |
| 收盘 | 最近有效交易日的最终收盘数据，且当前处于日终、休市日或非交易时段 | 可作为收盘参考，不得标为实时；用于“上一个有效收盘价”时需满足同一市场与证券。 |
| 暂无数据 | 无法获得、无法验证、字段不完整或数据冲突未解决 | 不展示虚构价格；相关实时估值显示“暂无数据/未确认”。 |

午间休市应另显示市场阶段“午间休市”，不将上午最后一笔行情称为当日“收盘”。

### 5.2 过期阈值与异常

- 在正常交易时段内，行情数据时间距离系统时间**超过 5 分钟**，必须标记为“延迟（异常）”。公开行情无论刷新频率如何，均不得因本地抓取及时而标为“实时”。
- “延迟（异常）”行情可显示最后有效报价、来源和时间，但不得作为“实时”报价；默认不用于需要当前有效价格的自动估值，除非用户明确选择该估值口径。
- 数据时间缺失、未来时间、抓取时间早于合理阈值或来源缺失时，状态为“暂无数据”。
- 过期阈值以数据时间为主、抓取时间为辅助；只刷新本地缓存而没有更新行情时间，不能消除过期状态。

## 6. 数据源规则

### 6.1 来源优先级

| 优先级 | 定义 | 使用规则 |
| --- | --- | --- |
| 一级：正规 API | 与交易所、官方机构或有合法授权的专业供应商提供的稳定 API | 作为优先报价与权威结构化数据来源；须符合授权范围和服务条款。 |
| 二级：公开行情 | 交易所/官方公开页面或可合法访问、可验证的公开行情与公告渠道 | 在一级不可用或未覆盖时使用；须保存原始链接、抓取时间与适用限制。 |
| 三级：备用数据 | 合规的聚合或备用公开数据 | 仅在更高优先级不可用时使用，显著标注备用来源和质量状态。 |

社区（如雪球、韭研公社）仅可作为“社区观点”资讯来源，不进入行情、官方公告或客观事实的权威来源链。

### 6.2 数据冲突处理

1. 先验证证券代码、市场、证券类型、字段完整性、数据时间与来源授权；不匹配的数据直接拒绝。
2. 在相同或可比较的行情时间（相差不超过 60 秒）内，优先采用优先级更高且未过期的数据源。
3. 若高优先级数据已过期而低优先级数据有效，可启用低优先级故障切换，但必须显示实际 `source`、时间和“备用/延迟”状态。
4. 两条可比较报价的价格差异超过 `max(0.01 元, 0.5%)` 时，标记“数据冲突”；未经可验证的来源确认前，状态改为“暂无数据/未确认”，不得静默混合、平均或选择更有利的价格。
5. 资讯、公告和事件以来源、原文 URL/标识、发布时间和内容哈希去重；冲突事件保留各来源并标注确认状态，而非改写为单一事实。

## 7. AI 分析规则

AI 每日复盘只可使用本地数据库中已保存、具有来源与时间元数据的行情、资讯、公告、事件、交易和用户笔记作为输入。每次输出必须有以下三个分区：

| 分区 | 允许内容 | 约束 |
| --- | --- | --- |
| `FACTS` | 数据库中可追溯的客观记录 | 每项关键事实应关联证据标识或来源；社区内容不能作为事实。 |
| `INFERENCES` | 基于事实的 AI 分析、可能的关联或解释 | 使用不确定性语言，明确为推断，不得替代事实。 |
| `RISKS` | 数据时效、来源冲突、事件、分析局限和未知因素 | 对影响结论的缺失或未确认数据必须披露。 |

禁止输出或暗示收益预测、收益保证、必涨预测、确定性价格目标、推荐买卖、必买/必卖结论。证据不足时，AI 必须写“暂无数据”或“未确认”，而不是补足叙事。

## 8. 系统时间规则

| 时间类型 | 定义 | 使用方式 |
| --- | --- | --- |
| 数据时间 | 数据源声明的行情发生时间、资讯发布时间或事件生效时间 | 判断新鲜度、交易日归属、先后顺序和证据时效；原样保存并标准化时区。 |
| 抓取时间 | 本应用从外部数据源成功取得该记录的时间 | 判断获取延迟、排障和缓存有效性；不能代替数据时间。 |
| 系统时间 | 本地应用执行计算、刷新或渲染时的当前时间 | 用于计算过期阈值和界面提示；不能覆盖外部数据时间。 |

- 所有存储时间应采用带时区的 ISO 8601 UTC；交易日和界面展示转换为 `Asia/Shanghai`。
- 时间缺失、时钟明显未来、数据时间晚于抓取时间且超出允许传输误差时，标记“未确认”并禁止作为实时依据。
- 规则计算应记录所使用的时间字段与规则版本，保证后续复算结果可解释。

## 9. 规则变更与审计

成本法、手续费口径、行情阈值、数据源优先级和 AI 限制属于 V1.0 基线。任何修改都必须版本化、记录生效时间和迁移/重算影响；历史交易流水不得因规则变更而被静默覆盖。

## 10. 备份与导出规则

- V1.0 每天自动备份一次 SQLite 数据库。备份任务应记录执行时间、结果、文件完整性校验与失败原因；失败时不得影响正在使用的原数据库。
- 默认备份目录为：
  - macOS：`~/Documents/AStock-AI-Workbench/backups`
  - Windows：`Documents/AStock-AI-Workbench/backups`
- 自动备份只备份 SQLite 数据库，不以 JSON/CSV 文件取代可恢复备份。应用应清楚区分“数据库备份”和“数据导出”。
- V1.0 支持用户显式导出 JSON 与 CSV。导出前须告知导出范围、目标位置和隐私风险；导出失败不得修改原数据。
- 数据库恢复及历史交易流水导入均须由用户明确触发，并保留操作时间、来源与结果的审计记录。

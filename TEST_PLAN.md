# 测试计划

## 1. 目标

验证资产核算、A 股 T+1、数据真实性标识、来源追溯与 AI 输出边界，确保应用在没有可靠数据时不展示虚假结论。

## 2. 测试层次

| 层次 | 范围 | 重点 |
| --- | --- | --- |
| Rust 单元测试 | 领域计算、校验、值对象 | 成本、费用、盈亏、精度、T+1、边界值 |
| SQLite 迁移与 CRUD 测试 | 迁移器、数据库服务层 | 迁移幂等、核心表完整、证券/持仓/交易流水的创建、查询、更新与删除 |
| Adapter 契约测试 | 各外部 Provider Adapter | 统一字段映射、来源与时间必填、错误/限流/缺数据行为 |
| SQLite 集成测试 | 仓储、迁移、事务、索引 | 外键、去重、交易写入原子性、数据可重算 |
| Tauri 集成测试 | command 边界与权限 | 参数校验、最小权限、无交易能力、密钥不泄露 |
| React 组件/E2E | 关键用户流程与状态呈现 | 数据质量标识、空态、持仓/交易管理、复盘分区 |
| 人工验收 | 跨平台发布候选 | Windows/macOS、升级/备份/恢复、可用性与文案 |

## 3. 必测业务场景

1. 买入后当日卖出被拒绝；下一交易日仅允许卖出未卖出的数量。
2. 多次买入、部分卖出、费用、零持仓、公司代码重复/市场不同等场景的成本和盈亏可复算。
3. 价格/金额/数量高精度与四舍五入一致；禁止浮点累积误差。
4. 无行情、过期行情、抓取失败、非交易时段、来源缺失时，界面显示“暂无数据”或“未确认”，不生成价格。
5. 每条股票、指数、ETF 行情均有代码、名称、市场、价格、涨跌幅、行情时间、抓取时间、来源、延迟状态。
6. 官方与媒体资讯能追溯原始链接；雪球、韭研公社内容始终显示“社区观点”。
7. 日历中未确认事件不能被表述为已发生；来源、事件日期、状态可查。
8. AI 复盘只在证据可用时生成，且固定含 `FACTS`、`INFERENCES`、`RISKS`；测试拒绝收益承诺、必涨预测、必买推荐和无证据事实。
9. 源码、构建物、日志和 SQLite 文件中均不出现 API Key；密钥读取失败有安全提示。
10. 应用不存在下单、券商交易授权或自动交易入口/API。
11. 数据库迁移重复执行后只保留一个迁移记录，且 `securities`、`holdings`、`transactions`、`cash_accounts`、`market_snapshots`、`market_quotes`、`data_sources` 均存在。
12. 数据库服务层的证券、持仓、交易流水 CRUD 测试通过；本阶段不得由交易流水触发成本、盈亏或 T+1 计算。
13. 旧数据库从 `001` 升级至 `002` 后，既有证券、持仓和交易流水均可读取；新增的交易状态、证券规则、费用、持仓总成本和公司行动表可用，且不丢失原字段值。
14. Portfolio Engine 使用 decimal 验证单次买入、多次加仓、部分卖出、全部卖出、买卖费用、取消交易、股票 T+1 和 ETF `UNKNOWN` 规则；每个适用场景校验数量、成本金额、平均成本、已实现盈亏及未实现盈亏。
15. Market Data Adapter 契约验证必填证券、价格、昨收、涨跌、成交、数据时间、抓取时间、来源及状态字段；源失败、空响应、缺失/过期时间均返回 `NO_DATA`，不生成替代报价；公开行情在交易中始终为 `DELAYED`，仅获授权的实时来源才可按 1/5 分钟阈值标记实时/延迟。
16. Dashboard UI 经 Tauri Command 读取资产摘要与市场快照；尚无完整可追溯输入时，六项资产指标与四个指数价格/涨跌幅均显示“暂无数据”，每张指数卡始终显示来源、状态、更新时间；未实现模块的导航页显示 `Coming Soon`。
17. Portfolio UI 经 Tauri Command 完成本地持仓新增、修改、删除、查询；成本金额、可卖数量、市值和今日盈亏只在 Rust Portfolio/Market 服务中计算。无持仓显示“暂无持仓”，无有效行情时当前价、市值、今日盈亏、总盈亏与涨跌幅显示“暂无数据”，不得由前端填补或运算。
18. Settings UI 经 Tauri Command 读取 Tushare 的安全配置状态、系统数据库/行情状态和最后同步时间；任何 Token 均不得出现在 Command 返回值、前端、SQLite 或日志。手工新增现金账户只接受 `CNY` 与非负精确 decimal 金额。SQLite 立即备份须生成可重新打开的一致性副本，保存至 Documents/AStock-AI-Workbench/backups，且不能覆盖已有备份。
19. News Service 的资讯 CRUD、关联证券校验、持仓关联查询和 004 迁移均须通过。每条测试记录必须具备来源、`OFFICIAL`/`MEDIA`/`COMMUNITY` 类型、带时区发布时间、抓取时间、摘要和 HTTP(S) 原文地址；页面只读取 Rust Command 返回的持仓关联资讯，空态显示“暂无与持仓关联的资讯”，社区记录显式展示“社区观点 · COMMUNITY”。
20. Review Service 的 005 迁移、同日复盘生成/读取、市场快照关联和持仓今日盈亏贡献排序均须通过。复盘仅能读取已保存的 Portfolio、市场快照和持仓关联资讯；没有完整账户汇总、有效指数或当日快照时分别显示“暂无数据/未确认”。风险提示只可输出数据状态的事实，测试拒绝买卖建议、收益预测与收益承诺。Review UI 经 Tauri Command 展示日期、账户表现、市场表现、持仓影响与风险事实。
21. AI Service 的 006 迁移、无完整运行时配置状态、Provider Adapter 契约、`FACTS`/`INFERENCES`/`RISKS` JSON 结构、持久化审计字段与禁止词拦截均须通过。AI 只接收已保存每日复盘（含快照）、Portfolio 和持仓关联资讯；UI 不能直接调用 Provider。任何输出包含买入、卖出、加减仓、推荐、目标价、收益预测/承诺、保证收益或必涨等内容时必须拒绝保存和展示。API Key 不得出现在进程参数、日志、SQLite、Command 返回值或前端。
22. Event Service 的 007 迁移、事件 CRUD、六类事件类型、三种确认状态、带时区原始时间校验、状态过滤、持仓关联和日期排序均须通过。默认查询必须让当前持仓关联事件优先，随后按实际事件时间升序排列；宏观/FED 事件可不关联证券。事件页面仅通过 Rust Command 读取数据，空态显示“暂无事件”；不得自动推测事件日期、来源或确认状态。

## 4. 质量门禁

- 所有新增金融计算必须先有单元测试，覆盖正常、边界、失败和回归场景。
- 每个新增 Adapter 必须通过同一份契约测试；禁止在测试或演示中把 fixture 标为实时行情。
- 合并前运行格式化、静态检查、单元测试与相关集成/E2E 测试。
- 发布前在 Windows/macOS 做冒烟测试，并验证数据库升级和回滚/备份恢复策略。

## 5. 测试数据策略

领域测试可使用明确标识为“测试夹具”的静态样本，仅用于验证计算与解析，不得进入产品行情展示。Adapter 测试使用录制响应、模拟服务器或官方沙箱；生产展示只使用可追溯的实际获取结果。

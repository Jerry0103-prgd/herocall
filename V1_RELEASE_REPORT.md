# AStock-AI-Workbench V1.0 发布前验收报告

**验收日期：** 2026-08-08

**候选版本：** `0.9.0`（提交前基线 `91b54b0`）

**验收环境：** macOS 15.4.1、Apple Silicon（`aarch64`）、SQLite 本地应用数据目录
**结论：** **不通过，禁止作为 V1.0 正式版本发布。**

## 1. 方法与证据

- Rust：`cargo fmt --check`、`cargo test --offline`（34/34 通过）、`cargo build --release --offline`。
- 前端：`npm run build` 通过。
- macOS：已验证 Release `.app`、DMG（UDZO）及已安装应用的两次启动；安装应用二进制与 Release 包一致。
- 本地生产 SQLite：位于 `~/Library/Application Support/com.astockai.workbench/astock-ai-workbench.sqlite3`，已应用 `001`–`008` 迁移；`initialization_completed=true` 在重启后仍保留。
- 人工界面抽检：Dashboard 显示“暂无数据”，未以零或演示价格替代缺失的资产和指数行情。
- 安全扫描：源码、Git 历史和前端构建产物的常见真实凭据模式扫描结果均为 0；没有自动下单/券商 API 命令匹配。此扫描不等同于第三方渗透测试。

状态说明：**通过**=本次有足够证据满足；**部分通过**=仅模块/静态证据，尚缺端到端或真实数据证明；**未通过**=当前产品能力不满足；**阻断/未执行**=缺少必要环境或基线，不能判定通过。

## 2. 48 项验收矩阵

| 类别 | 编号 | 状态 | 本次结论与证据 |
| --- | --- | --- | --- |
| Dashboard | FUN-01 | 未通过 | `dashboard_service` 当前刻意返回空资产摘要；无法核对总资产。 |
| Dashboard | FUN-02 | 未通过 | 没有已保存有效报价或持仓市值聚合。 |
| Dashboard | FUN-03 | 未通过 | 没有可追溯的今日盈亏聚合。 |
| Dashboard | FUN-04 | 未通过 | 没有总盈亏聚合与对账路径。 |
| Dashboard | FUN-05 | 未通过 | 没有收益率聚合与口径展示。 |
| Dashboard | FUN-06 | 部分通过 | 四个指数标识和 `NO_DATA` 空态正确；无真实价格、来源和时间可验。 |
| 持仓 | FUN-07 | 部分通过 | Rust/页面支持名称、代码、市场、类型、数量、成本；未录入行业和概念。 |
| 持仓 | FUN-08 | 未通过 | 可修改数量/成本但没有交易流水审计边界。 |
| 持仓 | FUN-09 | 未通过 | 删除按钮没有确认流程，且未证明交易历史保留策略。 |
| 持仓 | FUN-10 | 未通过 | 页面未提供名称、代码或市场查询。 |
| 持仓 | FUN-11 | 部分通过 | 移动平均成本单元测试通过；没有交易流水 UI/端到端写入。 |
| 持仓 | FUN-12 | 部分通过 | Rust 接口可处理有效行情；本机无报价，无法实际对账。 |
| 持仓 | FUN-13 | 部分通过 | Portfolio Engine 盈亏测试通过；产品没有完整交易录入与行情链路。 |
| 交易 | FUN-14 | 未通过 | 没有买入交易的 Tauri Command 或 UI。 |
| 交易 | FUN-15 | 未通过 | 没有卖出交易的 Tauri Command 或 UI。 |
| 交易 | FUN-16 | 部分通过 | T+1 领域单元测试通过；没有用户可执行的卖出流程。 |
| 交易 | FUN-17 | 部分通过 | 可卖数量领域单元测试通过；没有端到端交易录入。 |
| 交易 | FUN-18 | 部分通过 | 领域/数据库测试覆盖部分校验；完整交易输入的失败原子性未验。 |
| 交易 | FUN-19 | 未通过 | 没有面向用户的交易审计查询与修改/删除处理流程。 |
| 行情 | FUN-20 | 部分通过 | Adapter 规范化测试覆盖必填元数据；没有股票行情页面或已保存真实行情。 |
| 行情 | FUN-21 | 部分通过 | 证券/ETF 模型和指数空态存在；无真实指数或 ETF 样本。 |
| 行情 | FUN-22 | 部分通过 | 人工抽检和单元测试确认缺失时显示“暂无数据”；真实展示链路未验。 |
| 行情 | FUN-23 | 部分通过 | `NO_DATA`、延迟、收盘、过期状态单元测试通过；未做生产数据源故障演练。 |
| 行情 | FUN-24 | 部分通过 | 数据模型强制来源/时间字段；本机没有可审计报价记录。 |
| 新闻 | FUN-25 | 部分通过 | 新闻存储测试要求完整字段；没有外部新闻抓取或真实页面记录。 |
| 新闻 | FUN-26 | 部分通过 | `OFFICIAL`、`MEDIA`、`COMMUNITY` 类型模型和页面样式存在；无真实样本。 |
| 新闻 | FUN-27 | 通过 | 社区类型的显式标识由服务和页面实现，新闻测试通过。 |
| 新闻 | FUN-28 | 部分通过 | 支持持仓关联读取；未提供完整日期/类别查询且无真实原文样本。 |
| AI | FUN-29 | 通过 | Provider 输出结构校验测试通过，UI 固定渲染 `FACTS`、`INFERENCES`、`RISKS`。 |
| AI | FUN-30 | 部分通过 | 输入绑定本地结构化数据；没有已配置 Provider 的真实证据抽检。 |
| AI | FUN-31 | 部分通过 | 风险段结构和测试存在；没有真实生成样本。 |
| AI | FUN-32 | 通过 | 禁止词检查测试通过，提示与持久化校验均拒绝推荐/收益预测语言。 |
| 数据质量 | DATA-01 | 通过 | Adapter 失败测试返回 `NO_DATA`，Dashboard 空态人工抽检通过。 |
| 数据质量 | DATA-02 | 部分通过 | 无数据/来源失败单元测试通过；未进行断网桌面端人工测试。 |
| 数据质量 | DATA-03 | 通过 | 数据源失败与空响应测试通过，未生成替代报价。 |
| 数据质量 | DATA-04 | 通过 | 时间状态测试覆盖过期、延迟、收盘与授权实时边界。 |
| 安全 | SEC-01 | 通过 | 源码、Git 历史和 `frontend/dist` 的常见凭据模式扫描均为 0。 |
| 安全 | SEC-02 | 部分通过 | 未发现本地应用日志；代码将 AI 授权头经 stdin 传递。未在配置真实 Key 的运行时做日志演练。 |
| 安全 | SEC-03 | 未通过 | SQLite 未存 API Key/券商权限，但生产数据库权限为 `-rw-r--r--`，不满足最小文件权限要求。 |
| 安全 | SEC-04 | 未通过 | 自动化测试仅验证生成备份；没有恢复功能、恢复演练或实际备份文件。 |
| 性能 | PERF-01 | 阻断/未执行 | 预检“启动到进程出现”为 69 ms，非“可交互主界面”P95；缺少 1,000 笔/100 持仓基线。 |
| 性能 | PERF-02 | 阻断/未执行 | 没有页面导航/筛选 P95 采样及规定数据基线。 |
| 性能 | PERF-03 | 阻断/未执行 | 当前未接入页面行情刷新，无法测量刷新 SLA。 |
| 发布 | REL-01 | 部分通过 | Apple Silicon `.app` 与 DMG 已构建、挂载并启动；未做干净环境卸载测试，且仅 ad-hoc 签名、未公证。 |
| 发布 | REL-02 | 阻断/未执行 | 仅有 Windows GitHub Actions 方案，未生成或验证 Windows 安装包。 |
| 发布 | REL-03 | 部分通过 | macOS 有本地数据启动与重启通过；未完成无网络、首次启动人工和 Windows 验证。 |
| 发布 | REL-04 | 部分通过 | 初始化完成标志重启保留；未验证持仓/交易/备份恢复的完整保存。 |
| 发布 | REL-05 | 部分通过 | `001` 升级至最新迁移的自动化测试通过；没有上一正式版本的安装升级和恢复演练。 |

**汇总：** 7 项通过、24 项部分通过、13 项未通过、4 项阻断/未执行；因此不满足 `ACCEPTANCE_CRITERIA.md` 要求的“48 项全部通过”。

## 3. 数据真实性抽检

本次不能形成“至少 3 只真实 A 股证券”的通过样本。生产 SQLite 中 `securities=0`、`market_snapshots=0`、`market_quotes=0`，且 Dashboard 明确显示“行情未接入页面刷新”。下表记录未执行的抽样结果，避免将测试夹具或外部记忆数据冒充软件数据。

| 证券代码 | 软件内数据源 | 软件显示 | 行情时间/抓取时间 | 状态 |
| --- | --- | --- | --- | --- |
| 600519 | 无记录 | 未在持仓/行情中展示 | 无 | `NO_DATA` / 不可验证 |
| 000001 | 无记录 | 未在持仓/行情中展示 | 无 | `NO_DATA` / 不可验证 |
| 510300 | 无记录 | 未在持仓/行情中展示 | 无 | `NO_DATA` / 不可验证 |

结论：未使用模拟、硬编码或旧数据伪装行情；但 FUN-20 至 FUN-24 的真实数据验收不能通过。

## 4. 发布与本地数据验证

- macOS DMG：`src-tauri/target/release/bundle/dmg/AStock AI Workbench_0.9.0_aarch64.dmg`，UDZO 校验及挂载内容验证完成。
- 本地数据库：`~/Library/Application Support/com.astockai.workbench/astock-ai-workbench.sqlite3`，非开发目录；迁移为 `001`–`008`。
- 初始化恢复：`initialization_completed=true` 经两次应用启动后保持。
- 安装包签名：ad-hoc 签名，Gatekeeper 验证未通过/不可用；没有 Developer ID 签名和 Apple 公证。
- Windows：工作流已准备，但没有 Windows 运行、安装、升级或卸载证据。

## 5. 已知问题（发布阻断项）

1. Dashboard 没有资产摘要、估值、盈亏或收益率聚合；当前恒为“暂无数据”。
2. 没有交易记录 UI/Tauri Command，无法验收买卖、交易审计、完整数据校验与端到端 T+1。
3. 没有页面触发的真实行情刷新或三只证券的可追溯行情样本。
4. 新闻没有外部抓取与真实来源样本；查询能力未满足按日期/类别的完整要求。
5. 持仓删除没有确认，编辑数量/成本缺乏交易流水审计边界。
6. SQLite 生产文件为全局可读（`-rw-r--r--`），且没有备份恢复能力/演练。
7. 性能未按规定的 1,000 笔交易、100 持仓、12 个月资讯基线采样。
8. Windows 安装包未生成或验证；macOS 包未进行 Developer ID 签名与公证。

## 6. 发布建议

**不建议发布为 V1.0，也不应标记为正式 Release Candidate。** 可保留 `v0.9.0-release-build` 作为工程预览包。只有在所有 48 项验收项获得可追溯的通过证据、真实行情与完整交易链路完成、数据库权限/备份恢复完善，并完成 macOS 签名公证和 Windows 实机发布验证后，才可进入 V1.0 最终发布验收。

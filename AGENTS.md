# AStock-AI-Workbench 工程约定

本仓库用于构建“**A股个人 AI 投研工作台**”。它是供个人长期使用的本地投研工具，不是自动交易软件。

## 不可违反的原则

1. **不得制造金融数据。** 不得用模拟、猜测、占位或缓存过期数据伪装为真实或实时行情。
2. **不得隐藏数据来源。** 面向用户展示的数据应能追溯到提供方；来源不可用时明确标注。
3. **所有行情必须记录 `source` 和 `timestamp`。** 同时记录本地 `fetched_at`、延迟状态及适用交易日/时段。
4. **所有金融计算必须有测试。** 成本、可卖数量、已实现/未实现盈亏、收益率及四舍五入规则均须覆盖正常与边界场景。
5. **所有外部接口必须使用 Adapter 设计。** 业务层不得直接依赖具体供应商的接口格式；Adapter 负责鉴权、限流、映射、错误与来源元数据。
6. **API Key 不得写入源码。** 使用系统密钥链、环境变量或本地受保护配置；禁止提交密钥、令牌或用户隐私数据。
7. **AI 分析必须区分事实和推断。** 输出采用 `FACTS`、`INFERENCES`、`RISKS` 三个明确部分，分别保留可追溯证据。
8. **不确定数据应显示“暂无数据”或“未确认”。** 不得以推测补齐字段；社区内容必须标注“社区观点”，不得作为事实陈述。

## 产品与安全边界

- 默认只读；不实现自动下单、不申请或保存券商交易权限、不连接下单接口。
- 交易记录仅用于用户手工记账与持仓核算，不是交易指令。
- A 股交易计算须遵守 T+1：当日买入的股票当日不可卖出；卖出数量不得超过可卖数量。
- 优先采用本地存储与最小化数据收集。导入、导出、删除和备份都应由用户显式触发。

## 开发约定

- 技术栈目标：Tauri 2、React、TypeScript、Rust、SQLite，支持 Windows/macOS 本地运行。
- 保持领域模型、数据 Adapter、应用服务、界面层的单向依赖；跨层访问须通过清晰接口。
- 时间以带时区的 ISO 8601 表示；交易日和市场时区统一按 `Asia/Shanghai` 解释。
- 金额、价格、数量不得使用二进制浮点数进行核心核算；采用定点整数或精确 decimal 方案，并在边界处统一格式化。
- 新增数据源、计算规则、AI 提示或迁移时，先更新对应设计文档和测试计划。
